node_modules/
.env
.env.bak.*
.env.local
.env.prod
**/.env.prod
dist/
.next/
coverage/
*.log
server/api/uploads/
# PHOTO_DIR resolves relative to cwd; running vitest from inside server/api
# (rather than the repo root) doubles the path from .env's
# ./server/api/uploads into server/api/server/api/uploads. Known bug, not
# yet fixed — this just stops the resulting junk from being committed.
server/api/server/
server/aiml/.venv/
server/aiml/__pycache__/
**/__pycache__/
client/app/.expo/
client/app/android/
test-results/
playwright-report/
client/app/src/db/generated/
client/app/src/db/generated-node/
client/app/prisma/.env
client/app/prisma/bhaav.db

# OS / editor / local tooling
.DS_Store
**/.DS_Store
.superpowers/
.claude/
**/.claude/

# DB snapshots — contain recycler_account password hashes
backups/

# graphify knowledge-graph output — generated, includes a 3.4MB semantic cache
graphify-out/

# Agent coordination log — local process scaffolding, same class as .claude/ and .superpowers/
AgentContext.md
